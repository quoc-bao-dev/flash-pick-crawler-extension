
export const crawlProductFlashSale = async () => {
    // const session = await getSession()
    // const listPromotionId = session.data.sessions.map((session: any) => session.promotionid);

    // console.log({listPromotionId});

    console.log("crawlProductFlashSale");
// listPromotionId.forEach(async (promotionId: string) => {
//     const itemIds = await getAllItemIds(promotionId)
//     console.log({itemIds});
//     const data =  itemIds.data ? itemIds.data.selling_out_item_brief_list.map((item: any) => item.itemid) : [];
//     console.log( "itemIds.data.selling_out_item_brief_list ",itemIds.data.selling_out_item_brief_list);
//     console.log(data)
// })
}